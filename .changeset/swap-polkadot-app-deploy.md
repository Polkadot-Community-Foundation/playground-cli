---
"playground-cli": patch
---

Switch the deploy engine dependency from the closed-source `bulletin-deploy` to its open-source twin `@parity/polkadot-app-deploy` (code-identical at 0.10.0). No behavior change.
