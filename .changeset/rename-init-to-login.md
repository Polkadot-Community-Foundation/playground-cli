---
"playground-cli": minor
---

Rename the `init` command to `login`. Setup and mobile QR pairing now run via `playground login` (or `pg login`); the old `init` name is no longer available. `install.sh` and all in-CLI hints, warnings, and help text refer to `login`.
