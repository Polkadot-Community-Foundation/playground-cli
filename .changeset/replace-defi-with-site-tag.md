---
"playground-cli": minor
---

Replace the `defi` app tag with `site`. The `site` tag is now the first option offered in the deploy/decentralize tag picker and the `--tag` flag, and `defi` is no longer accepted.
