---
"playground-cli": minor
---

Add `playground init` to start a new project from the playground starter template. It's a shorthand for `playground mod playground-template`, giving new users a one-command way to scaffold an app to build on.
